---
"khotan-data": patch
---

Expand the generated plug pagination and retry helpers with inline per-call pagination inspectors, link/JSON:API/envelope strategies, absolute next URL support, inter-page delays, and `shouldRetry(response, body)` for body-level throttles on successful HTTP responses.
